apiVersion: apps/v1
kind: Deployment
metadata:
  name: goal-service
  namespace: wealthcare
  labels:
    app: goal-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: goal-service
  template:
    metadata:
      labels:
        app: goal-service
        version: v1
    spec:
      containers:
      - name: goal-service
        image: wealthcare/goal-service:1.0.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8083
          name: http
          protocol: TCP
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        - name: CUSTOMER_SERVICE_URL
          value: "http://customer-service:8082"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8083
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8083
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: data
          mountPath: /app/data
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: goal-service-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: goal-service
  namespace: wealthcare
  labels:
    app: goal-service
spec:
  type: ClusterIP
  ports:
  - port: 8083
    targetPort: 8083
    protocol: TCP
    name: http
  selector:
    app: goal-service
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: goal-service-pvc
  namespace: wealthcare
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: goal-service-config
  namespace: wealthcare
data:
  application.yml: |
    spring:
      application:
        name: goal-service
      datasource:
        url: jdbc:h2:file:/app/data/goal-db;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
        driver-class-name: org.h2.Driver
        username: sa
        password: 
      jpa:
        hibernate:
          ddl-auto: update
        show-sql: false
    server:
      port: 8083
    services:
      customer-service:
        url: http://customer-service:8082
    management:
      endpoints:
        web:
          exposure:
            include: health,info,metrics,prometheus
      endpoint:
        health:
          show-details: always
          probes:
            enabled: true
    logging:
      level:
        com.gan.wcare: INFO

# Made with Bob
